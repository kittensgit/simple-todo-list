import React from 'react';

const ToDoForm = () => {
    return (
        <div>
            <input placeholder="enter task" />
            <button>add</button>
        </div>
    );
};

export default ToDoForm;
